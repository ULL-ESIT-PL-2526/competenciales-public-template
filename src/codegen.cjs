/*
JavaScript code generation using Babel generator.
*/

const generate = require("@babel/generator").default;

function generateJavaScript(ast, options = {}, source = '', sourceFile = '') {
    /* fill here */
}

module.exports = generateJavaScript;